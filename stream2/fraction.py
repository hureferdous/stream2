from fractions import Fraction
class Fraction:
    def __init__(self, numerator, denominator):
        self.num = numerator
        self.den = denominator
f = Fraction(1,2)
print(f)
f1 = Fraction(1,4)
print(f1)
